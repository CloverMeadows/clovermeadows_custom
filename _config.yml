# Site settings
title: "CloverMeadows"
description: Clover Meadows Targhees

# modify version numbers below when upgrading bootstrap
# only the assets directory is needed from the bootstrap-sass download
bootstrap-sass: bootstrap-4.0.0.beta
sass:
    sass_dir: /bootstrap-4.0.0.beta/assets/stylesheets
    style: :compressed

kramdown:
  input: GFM
  hard_wrap: true
  toc_levels: "2"

timezone: US/Mountain

exclude:
  - Gemfile
  - Gemfile.lock
  - Rakefile
  - README.md